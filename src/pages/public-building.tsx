import Layout from "@/components/Layout";
import PageIntro from "@/components/PageIntro";

export default function PublicBuilding() {
    return (
        <Layout>
            <PageIntro hook="" highlighted="MRR" heading="Road to $1k" paragraph="Wake up to an inbox full of new testimonial alerts. Share them everywhere in seconds. Get more visits, convert more, and close more deals. Senja makes it easy." />
        </Layout>
    )
}